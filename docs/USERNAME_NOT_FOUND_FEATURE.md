# ✅ قابلیت Skip کردن Username های NOT FOUND

## 🎯 مشکل

برخی username‌ها ممکن است:
- قدیمی باشند و صاحبشان آنها را تغییر داده باشد
- دیگر وجود نداشته باشند
- اشتباه تایپ شده باشند

## ✅ راه‌حل پیاده‌سازی شده

سیستم حالا **سه نوع نتیجه** برمی‌گرداند:

### 1. `SUCCESS` ✅
- پیام با موفقیت ارسال شد
- counter اکانت +1 می‌شود
- به username بعدی می‌رود

### 2. `NOT_FOUND` ⏭️
- Username وجود ندارد یا تغییر کرده
- **بلافاصله به username بعدی می‌رود**
- counter اکانت تغییر نمی‌کند (پیام ارسال نشده)
- فرآیند task ادامه می‌یابد

### 3. `FAILED` ❌
- خطای دیگری رخ داده (مثلاً privacy، rate limit)
- با اکانت دیگری retry می‌شود
- اگر با همه اکانت‌ها fail شد، skip می‌شود

## 🔍 نحوه تشخیص NOT_FOUND

سیستم این کلمات کلیدی را در error چک می‌کند:
```python
['username', 'not found', 'invalid', 'no user', 
 'user not found', 'could not find', 'no entity']
```

اگر **هر دو روش** (با @ و بدون @) این خطا را دادند:
→ مطمئن می‌شود username وجود ندارد
→ `NOT_FOUND` برمی‌گرداند

## 📊 مثال عملی

```
Task: ارسال پیام به 5 username

username1 → SUCCESS ✅
username2 → NOT_FOUND ⏭️ (skip فوری)
username3 → SUCCESS ✅
username4 → FAILED → retry با اکانت دیگر → SUCCESS ✅
username5 → NOT_FOUND ⏭️ (skip فوری)

Result:
- 3 پیام موفق
- 2 username not found (skip شدند)
- فرآیند کامل شد
```

## 🎯 مزایا

1. ✅ **سرعت بالا:** username های invalid را skip می‌کند
2. ✅ **بهینه‌سازی:** وقت و منابع را هدر نمی‌دهد
3. ✅ **ادامه task:** فرآیند متوقف نمی‌شود
4. ✅ **گزارش دقیق:** تفکیک بین "not found" و "failed"

## 📝 لاگ‌ها

```
⚠️ SKIP: oldusername not found (old/changed username), moving to next
⏭️ 2/5 processed (1 not found/failed)
```

## ⚙️ پیاده‌سازی

فایل: `src/smart_sender.py`

- تابع `_send_to_username` حالا string برمی‌گرداند
- لاجیک `send_all` سه حالت را handle می‌کند
- Counter اکانت فقط برای SUCCESS افزایش می‌یابد

