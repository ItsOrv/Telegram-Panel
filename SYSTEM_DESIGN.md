# 🎯 طراحی سیستم ارسال پیام با محدودیت

## 📋 قوانین سیستم

### 1. محدودیت هر اکانت
- هر اکانت فقط می‌تواند به **4 username** پیام بدهد
- بعد از 4 پیام، اکانت از لیست available حذف می‌شود

### 2. انتخاب رندوم
- اکانت‌ها به صورت **رندوم** از لیست available انتخاب می‌شوند
- این باعث می‌شود الگوی ارسال غیرقابل پیش‌بینی باشد

### 3. تایید ارسال
- بعد از هر پیام، ربات **چک می‌کند** پیام واقعاً ارسال شده یا نه
- اگر موفق بود → counter اکانت +1 می‌شود
- اگر ناموفق بود → username به لیست failed اضافه می‌شود

### 4. جایگزینی در صورت خطا
- اگر اکانت A نتواند به username پیام بدهد
- ربات اکانت B را انتخاب می‌کند و دوباره تلاش می‌کند
- اکانت B کار اکانت A را **کامل می‌کند**

### 5. چرخه مجدد
- وقتی همه اکانت‌ها به محدودیت 4 رسیدند
- ربات counter‌ها را **reset** می‌کند
- از اول شروع می‌کند

## 🔄 فلوچارت سیستم

```
Start
  ↓
Initialize: accounts_pool, usernames_queue, account_counters
  ↓
┌─────────────────────────────────────┐
│ While usernames_queue not empty    │
│   ↓                                 │
│   Select random account from pool   │
│   ↓                                 │
│   Get next username from queue      │
│   ↓                                 │
│   Send message                      │
│   ↓                                 │
│   ┌──────────────┐                 │
│   │ Success?     │                 │
│   └──────┬───────┘                 │
│      Yes │ No                       │
│          │  │                       │
│          │  └→ Add to failed_queue  │
│          │     Select new account   │
│          │     Retry                │
│          │                          │
│          ↓                          │
│   Increment account counter         │
│   ↓                                 │
│   ┌──────────────┐                 │
│   │ Counter >= 4?│                 │
│   └──────┬───────┘                 │
│      Yes │ No                       │
│          │  │                       │
│          │  └→ Continue             │
│          │                          │
│          ↓                          │
│   Remove account from pool          │
│   ↓                                 │
│   ┌──────────────┐                 │
│   │ Pool empty?  │                 │
│   └──────┬───────┘                 │
│      Yes │ No                       │
│          │  │                       │
│          │  └→ Continue             │
│          │                          │
│          ↓                          │
│   Reset all counters                │
│   Restore pool                      │
└─────────────────────────────────────┘
  ↓
End - Report results
```

## 💾 ساختار داده

### Account State
```python
{
    "session_name": "+989123456789",
    "client": TelegramClient,
    "messages_sent": 2,  # تعداد پیام‌های موفق
    "is_available": True
}
```

### Username Queue
```python
[
    "username1",
    "username2",
    "username3",
    ...
]
```

### Failed Attempts
```python
{
    "username1": ["account1", "account2"],  # اکانت‌هایی که fail کردند
}
```

## 🎯 مثال عملی

### Scenario: 3 اکانت، 10 username، limit=4

```
Round 1:
  Account2 (random) → username1 ✅ (counter: 1)
  Account2 → username2 ✅ (counter: 2)
  Account1 (random) → username3 ✅ (counter: 1)
  Account2 → username4 ✅ (counter: 3)
  Account3 (random) → username5 ❌ → Account1 → username5 ✅ (counter: 2)
  Account2 → username6 ✅ (counter: 4) → Account2 REMOVED
  Account1 → username7 ✅ (counter: 3)
  Account3 → username8 ✅ (counter: 1)
  Account1 → username9 ✅ (counter: 4) → Account1 REMOVED
  Account3 → username10 ✅ (counter: 2)

Result: All usernames sent
- Account1: 4 messages
- Account2: 4 messages
- Account3: 2 messages
```

## 🚀 مزایای سیستم جدید

1. ✅ **توزیع عادلانه** بار بین اکانت‌ها
2. ✅ **جلوگیری از محدودیت** Telegram با توزیع رندوم
3. ✅ **بازیابی خودکار** از خطاها
4. ✅ **مقیاس‌پذیری** برای task‌های سنگین
5. ✅ **گزارش دقیق** از عملکرد هر اکانت

